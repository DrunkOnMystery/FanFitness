const mongoose = require("mongoose");
const Schema = mongoose.Schema;
const Exercise = require("./Exercise");

// const workoutSchema = new Schema({
//     name: { type: String, required: true },

// })

// Your Team Is Hitting

// Team player grounds out:  
// Team player flies out or lines out:
// Team player pops out:
// Team player strikes out:
// Team player hits into a fielders choice:
// Team player hits into a double play:
// Team player hits into a triple play:

// Team player draws a walk:
// Team player hits a single:
// Team player hits a double:
// Team player hits a triple:
// Team player hits a homerun:
// Team player steals a base:

// Team scores a run:

// Your Team is on the field

// Opp draws a walk:
// Opp hits a single:
// Opp hits a double:
// Opp hits a triple:
// Opp hits a homerun:
// Opp gets hit by a pitch:
// Opp scores a run:
// Opp steals a base:

// Team records a groundout: 
// Team records a flyout:
// Team records a popout:
// Team records a lineout:
// Team records a strikeout:
// Team records a doubleplay:
// Team records a tripleplay:
// Team commits an error:
// Team commits a wild pitch or passed ball:
